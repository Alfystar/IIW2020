TODO LIST:
FIL
1) notify con pipe per il sizeofcache() mentre cresce (per non leggere sempre fstat) //poll?

EMA
OK 1) Risolvere il riconoscimento solo in base all'estensione per il 404
OK 2) Finire di estrapolare il quality factor dall'accept
~~ 2.5)capire come far visualizzare apposta la didascalia (alt_img)
[non serve pare] 3) Capire se si ha a che fare con un browser testuale o con GUI
OK 4) nell'Accept se il tipo non è image, allora mandare errore (non posso richiedere text/plain ad es. per un immagine);
    in caso contrario, capire il tipo verso cui convertire e la qualità.
OK 5) rendere configurabile la porta d'ascolto (il path è già fatto).

6) definire una fork iniziale per ritirare su il server se viene buttato giù [da ridiscutere]



https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types Per i tipi di Accept


